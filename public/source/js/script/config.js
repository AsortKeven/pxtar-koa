'use strict';

define(function(){
    // 前端模块路径配置。
    require.config({
        baseUrl: 'js/',
        paths: {
            'XkTool': 'script/xkTool',
            'Show': 'script/Show'
        }
    });
});
